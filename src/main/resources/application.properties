spring.jpa.hibernate.ddl-auto=none
spring.datasource.url=jdbc:mysql://localhost:3306/vb_spring-jwt
spring.datasource.username=testuser
spring.datasource.password=Testen01!
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL57Dialect

# Needs check if this is the right approach:
# This seems to prevent the com.fasterxml.jackson.databind.exc.InvalidDefinitionException
spring.jackson.serialization.fail-on-empty-beans=false

#Application properties
app.jwtSecret=%gt6743F#hfgtr
app.jwtExpirationInMs=604800000

